<!DOCTYPE html>
<html>
  <head>
    <title>Wracket</title>
    <%= csrf_meta_tags %>
    <%= yield :head %>
  </head>

  <body>
    <%= yield %>
    <script crossorigin src="<%= asset_url('app.js') %>"></script>
    <%= yield :scripts %>
  </body>
</html>
